#include <stdio.h>
#include<time.h>
/*more headers go here*/

/*betty style doc for function main goes here*/
int main(void)
{
	int n;

	srand(time(0));
	n=rand() - rand_max / 2;
	/*code goes here*/
	if (n > o)
		printf("is positive")
	else if (n == 0)
		printf("is zero")
	else
		printf("is negative")
	return (0);
}
