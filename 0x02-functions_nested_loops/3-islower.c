#include "main.h"
/**
 *islower - checks for a lower case character.
 *Return: Always 0.
 */
int _islower(int c)
{
	int a;

	int islower(int c);
	if ((int islower(int a)) == 1 )
	{
		a = 1;
		printf("%d", a);
	}
	else
	{
		a = 0;
		printf("%d", a);
	}
	return (a);
}
