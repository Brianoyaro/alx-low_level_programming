#include "main.h"
char *cap_string(char *s)
/*{
	int a;

	for (a = 0; *s != '\0'; a++)
	{
		if (s[a] == ' '|| '\t' || '\n' || ',' || ';' || '.' || '!' || '?' || '"' || '(' || ')' || '{' || '}')
			if (s[a + 1] >= 'a' && s[a + 1] <= 'z')
				s[a + 1] -=  32;

	}
	return (s);
}*/
{
	int a = 0;

	while (s[a] != '\0')
	{
		
		a++;
	}
}
